type StarterBlock @entity {
  id: ID! #id is a required unique field
  height: String!
  transfers: [StarterTransfer]
}

type StarterTransfer @entity {
  id: ID! #id is a required unique field
  height: String!
  block: StarterBlock
  sender: String!
  recipient: String!
  amount: String!
}
