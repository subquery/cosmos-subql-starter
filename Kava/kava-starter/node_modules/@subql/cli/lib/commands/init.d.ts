import { Command } from '@oclif/core';
import { Template } from '../controller/init-controller';
export default class Init extends Command {
    static description: string;
    static flags: {
        force: import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
        location: import("@oclif/core/lib/interfaces").OptionFlag<string>;
        'install-dependencies': import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
        npm: import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
    };
    static args: {
        name: string;
        description: string;
    }[];
    private projectPath;
    private project;
    private location;
    private networkFamily;
    private network;
    run(): Promise<void>;
    observeTemplates(templates: Template[], flags: any): Promise<void>;
    setupProject(flags: any): Promise<void>;
}
