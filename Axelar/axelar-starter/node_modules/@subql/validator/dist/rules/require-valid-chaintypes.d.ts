import { SubstrateProjectManifestVersioned } from '@subql/common-substrate';
import { Context } from '../context';
import { Rule, RuleType } from './rule';
export default class RequireValidChainTypes implements Rule {
    type: RuleType;
    name: string;
    description: string;
    validateSubstrate(schema: SubstrateProjectManifestVersioned, ctx: Context): Promise<boolean>;
    validate(ctx: Context): Promise<boolean>;
}
