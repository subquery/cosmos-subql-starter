export declare abstract class ProjectManifestBaseImpl<D extends object> {
    definitions: object;
    description: string;
    repository: string;
    specVersion: string;
    abstract readonly deployment: D;
    toDeployment(): string;
    validate(): void;
}
export interface BaseDataSource<F = Record<string, unknown>, H extends BaseHandler<F> = BaseHandler<F>, T extends BaseMapping<F, H> = BaseMapping<F, H>> {
    name?: string;
    kind: string;
    startBlock?: number;
    mapping: T;
}
export interface BaseMapping<F, T extends BaseHandler<F>> {
    file: string;
    handlers: T[];
}
export interface BaseHandler<T> {
    handler: string;
    kind: string;
    filter?: T;
}
